
import React from 'react';

// Component deprecated and removed from UI as per user request.
// We keep the export to prevent breaking existing imports, but it renders nothing.
export const LaunchButton: React.FC<any> = () => {
  return null;
};
